package a3variableclass;

/**
 * @author Eva   Email:
 * @Description 变量类型
 * @data 2023/1/6 11:05
 */
public class AVariable {
    /*
    成员变量：
        实例变量（不以static修饰）
        类变量（以static修饰）
    局部变量：
        形参(方法、构造器中定义的变量)
        方法局部变量(在方法内定义)
        代码块局部变量(在代码块内定义)


    区别：
    1、位置，作用范围（方法体、语句块中；作用于）
    2、生命周期（生命期和主程序一样，随程序的销毁而销毁；局部变量在函数的退出或循环退出）
    3、存储位置（全局变量存储在全局数据区中；局部变量存储在栈区）
    4、初始值（全局变量有，局部变量没有）
     */
}
